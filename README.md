# proveway-cost-box
Small front end project as an assessment. The goal is to create a web page containing a cost box of three different tiers, which expands on click and shows further customisation only using HTML, CSS and vanilla JavaScript. 
